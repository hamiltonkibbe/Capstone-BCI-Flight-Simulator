#ifndef CSV_VV_HPP
#define CSV_VV_HPP

#include <fstream>
#include <string>
#include <sstream>
#include <vector>

//function to convert CSV to vector of vector
void csv_vv(const char* f_name, std::vector<std::vector<double> > &dest);
void csv_vv(const char* f_name, std::vector<std::vector<double> > &dest, char delim);

#endif